import React from 'react';

const Search = (props) =>{
    return(
        <div>
            <form>
            <input type='text' name='playerName'/>
            <button>Search</button>
            </form>
        </div>
    )
}

export default Search;